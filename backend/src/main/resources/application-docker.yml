spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/monidb
    username: moniuser
    password: monipass
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true